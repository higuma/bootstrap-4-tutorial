extends ../template-cdn-en
include ../mixins-en

mixin img286x180top
  img.card-img-top(src="../img/286x180.svg" alt="Card image cap")

mixin quoteFooter
  footer.blockquote-footer Someone famous in
    cite(title="Source Title") Source Title

mixin lastUpdated
  small.text-muted Last updated 3 mins ago

mixin textLastUpdated
  p.card-text
    +lastUpdated

block head
  title Bootstrap 4 tutorial: Components (3): Cards

block body
  - const contexts = ["primary", "secondary", "success", "danger", "warning", "info", "light", "dark"];
  - const sampleText = "Some quick example text to build on the card title and make up the bulk of the card's content."
  - const sampleText2 = "With supporting text below as a natural lead-in to additional content."
  - const lorem = "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer posuere erat a ante."
  div.container
    +pageHeader("components-3")
      p.lead Components (3): 
        a(href="#cards") Cards

    h2.mt-2#cards
      +extLink("https://getbootstrap.com/docs/4.1/components/card/") Cards
    h4.mt-3#example
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#example") Basic example
    div.card.my-3(style="width: 18rem;")
      +img286x180top
      div.card-body
        h5.card-title Card title
        p.card-text= sampleText
        a.btn.btn-primary(href="#") Go somewhere
    +collapseButton("#basic-example-structure") Structure
    ul.struct.mt-2.collapse#basic-example-structure
      li Card 
        code div.card
        +attrBtn(".a1-1")
        ul
          li.att.collapse.a1-1 Attribute (sets width) 
            code style="width: 18rem;"
          li Image (top) 
            code img.card-img-top
            +attrsBtn(".a1-2")
            ul
              li.att.collapse.a1-2 Attribute 
                code src="..."
              li.att.collapse.a1-2 Attribute 
                code alt="..."
          li Body 
            code div.card-body
            ul
              li Title 
                code h5.card-title
                ul
                  li.txt Title text 
                    code Card title
              li Text content 
                code p.card-text
                ul: li.txt Text 
                  code Some quick example text ...
              li Button 
                code a.btn.btn-(primary|secondary|...)
                +attrBtn(".a1-3")
                ul
                  li.att.collapse.a1-3 Attribute 
                    code href="..."
                  li.txt Text 
                    code Go somewhere
    h4.mt-4#content-types
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#content-types") Content types
    p.my-2 Content classes for building cards (also can be used for other purposes).
    h5.mt-3#body
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#body") Body
    p.my-2 Use 
      code div.card-body
    div.card.mb-3
      div.card-body This is some text within a card body.
    +collapseButton("#example-body-structure") Structure
    ul.struct.mt-2.collapse#example-body-structure
      li
        code div.card
        ul
          li
            code div.card-body
            ul
              li.txt Text 
                code This is some text within a card body.
    h5.mt-4#titles-text-and-links
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#titles-text-and-links") Title, text, and links
    div.card.my-3(style="width: 18rem;")
      div.card-body
        h5.card-title Card title
        h6.card-subtitle.mb-2.text-muted Card subtitle
        p.card-text= sampleText
        a.card-link(href="#") Card link
        a.card-link(href="#") Another link
    +collapseButton("#example-typos-structure") Structure
    ul.struct.mt-2.collapse#example-typos-structure
      li Title 
        code (h1|h2|h3...).card-title
        |  (used 
        code h5.card-title
        |  above)
      li Subtitle 
        code (h1|h2|h3...).card-subtitle
        |  (used 
        code h6.card-subtitle
        |  above)
      li Link 
        code (a).card-link
      li Text 
        code (p).card-text
    h5.mt-4#images
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#images") Images
    p.my-2 Use 
      code img.card-img-top
      |  to set an image on the top.
    div.card.mt-2.mb-3(style="width: 18rem;")
      +img286x180top
      div.card-body
        p.card-text= sampleText
    +collapseButton("#example-images-structure") Structure
    ul.struct.mt-2.collapse#example-images-structure
      li
        code div.card
        ul
          li
            code img.card-img-top
          li
            code div.card-body
    p.mt-3.mb-1 Note: It is set as a first child of a card (not within a card body). See 
      a(href="#images-1") Using images
      |  for detail.
    h4.mt-4#list-groups
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#list-groups") List groups
    h5.mt-3#flush-list Flush list
    div.card.mt-2.mb-3(style="width: 18rem;")
      ul.list-group.list-group-flush
        each text in ["Cras justo odio","Dapibus ac facilisis in","Vestibulum at eros"]
          li.list-group-item= text
    +collapseButton("#example-flush-list-structure") Structure
    ul.struct.mt-2.collapse#example-flush-list-structure
      li Flush list 
        code ul.list-group.list-group-flush
        ul
          li List items 
            code li.list-group-item
    h5.mt-4#list-header List header
    div.card.my-3(style="width: 18rem;")
      div.card-header Featured
      ul.list-group.list-group-flush
        each text in ["Cras justo odio","Dapibus ac facilisis in","Vestibulum at eros"]
          li.list-group-item= text
    +collapseButton("#example-list-header-structure") Structure
    ul.struct.mt-2.collapse#example-list-header-structure
      li Card 
        code div.card
        ul
          li Header (
            strong Featured
            | ) 
            code div.card-header
            |  (sticks to the next flush list)
          li Flush list (same as above)
    h4.mt-4#kitchen-sink
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#kitchen-sink") Kitchen sink
    div.card.my-3(style="width: 18rem;")
      +img286x180top
      div.card-body
        h5.card-title Card title
        p.card-text= sampleText
      ul.list-group.list-group-flush
        each text in ["Cras justo odio","Dapibus ac facilisis in","Vestibulum at eros"]
          li.list-group-item= text
      div.card-body
        a.card-link(href="#") Card link
        a.card-link(href="#") Another link
    +collapseButton("#example-kitchen-sink-structure") Structure
    ul.struct.mt-2.collapse#example-kitchen-sink-structure
      li Card 
        code div.card
        ul
          li Image (top) 
            code img.card-img-top
          li Card body 
            code div.card-body
            |  (stacks inner subcomponents)
            ul
              li Title 
                code h5.card-title
              li Text 
                code p.card-text
              li Flush list 
                code ul.list-group.list-group-flush
              li Links 
                code a.card-link
                |  (&times;2, arranged inline)
                +attrBtn(".a2-1")
                ul
                  li.att.collapse.a2-1 Attribute 
                    code href="..."
                  li.txt Text (e.g.) 
                    code Card link
    h4.mt-4.mb-3#header-and-footer
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#header-and-footer") Header and footer
    +collapseButton("#example-header-and-footer-structure") Structure
    ul.struct.mt-2.collapse#example-header-and-footer-structure
      li Card (parent) 
        code div.card
        ul
          li (Optional) header (as the first child) 
            code div.card-header
          li Body ( 
            code div.card-body
            |  , etc.)
          li (Optional) footer (as the last child) 
            code div.card-footer
    p.mt-3.mb-1 Header example (
      strong Featured
      | )
    div.card
      div.card-header Featured
      div.card-body
        h5.card-title Special title treatment
        p.card-text= sampleText2
        a.btn.btn-primary(href="#") Go somewhere
    p.mt-3.mb-1 Emphasizing the header text with 
      code h5.card-header
    div.card
      h5.card-header Featured
      div.card-body
        h5.card-title Special title treatment
        p.card-text= sampleText2
        a.btn.btn-primary(href="#") Go somewhere
    p.mt-3.mb-1 Using a 
      code blockquote
      |  in a card body (see 
      a(href="content-1.html#typo-blockquotes") Typography - Blockquotes
      |  )
    div.card
      div.card-header Quote
      div.card-body
        blockquote.blockquote.mb-0
          p= lorem
          +quoteFooter
    p.mt-3.mb-1 Setting both header+footer, also center-aligned with 
      code .text-center
    div.card.text-center
      div.card-header Featured
      div.card-body
        h5.card-title Special title treatment
        p.card-text= sampleText2
        a.btn.btn-primary(href="#") Go somewhere
      div.card-footer.text-muted 2 days ago
    h4.mt-4#sizing
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#sizing") Sizing
    p
      code div.card
      |  sets the width to 100% within the parent as default. There are various ways to specify widths you need.
    h5.mt-3#using-grid-markup
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#using-grid-markup") Using grid markup
    p.my-2 Responsive example: switchs layouts on md breakpoint (768px)
    div.row
      each n in [1,2]
        div.col-md-6.pb-3
          div.card
            div.card-body
              h5.card-title= `Special title treatment (${n})`
              p.card-text= sampleText2
              a.btn.btn-primary(href="#") Go somewhere
    +collapseButton("#example-using-grid-structure") Structure
    ul.struct.mt-2.collapse#example-using-grid-structure
      li
        code div.row
        ul
          each n in [0,1]
            li
              code div.col-md-6
              ul
                li
                  code div.card
                  ul
                    li Inside (same as above)
    h5.mt-3#using-utilities
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#using-utilities") Using utilities
    p.my-2 Use 
      code .w-(25|50|75|100)
      |  (see 
      a(href="utilities-4.html#sizing") Sizing utilities
      |  ).
    each width in [75, 50]
      p.mt-2.mb-0
        code= `div.card.w-${width}`
      div(class=`card w-${width}`)
        div.card-body
          h5.card-title Card title
          p.card-text= sampleText2
          a.btn.btn-primary(href="#") Button
    h5.mt-3#using-custom-css
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#using-custom-css") Using custom CSS
    p.my-2 Use 
        code= `style="width: ...;"`
    each width in [16, 24]
      p.mt-2.mb-0
        code div.card
        |  + 
        code= `style="width: ${width}rem;"`
      div.card(style=`width: ${width}rem;`)
        div.card-body
          h5.card-title Card title
          p.card-text= sampleText2
          a.btn.btn-primary(href="#") Button
    h4.mt-4#text-alignment
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#text-alignment") Text alignment
    p.mt-2.mb-0 Use 
      code .text-(left|center|right)
      |  (see 
      a(href="utilities-5.html#text-alignment") Text - alignment
      |  ).
    div.row
      each align in [".text-left", ".text-center", ".text-right"]
        div.col
          p.mt-2.mb-0
            code= `div.card${align}`
            - if (align == ".text-left")
              |  (default)
          div(class=`card${align.replace(/\./, " ")}` style="width: 18rem;")
            div.card-body
              h5.card-title Special title treatment
              p.card-text= sampleText2
              a.btn.btn-primary(href="#") Go somewhere
    h4.mt-4#navigation
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#navigation") Navigation
    p.my-2 Below are the examples using 
      a(href="components-9.html") Nav components
      | .
    each navType in ["tabs", "pills"]
      p.mt-2.mb-0
        code= `ul.nav.nav-${navType}.card-header-tabs`
      div.card.text-center
        div.card-header
          ul(class=`nav nav-${navType} card-header-tabs`)
            li.nav-item
              a.nav-link.active(href="#") Active
            li.nav-item
              a.nav-link(href="#") Link
            li.nav-item
              a.nav-link.disabled(href="#") Disabled
        div.card-body
          h5.card-title Special title treatment
          p.card-text= sampleText
          a.btn.btn-primary(href="#") Go somewhere
    h4.mt-4#images-1
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#images-1") Using images
    p.my-2 Use 
      code img.card-img-(top|bottom)
      |  to set image(s) as a header and/or a footer.
    p.mt-2.mb-1
      code img.card-img-bottom
    div.card
      img.card-img-top(src="../img/1000x100.svg" alt="Card image cap")
      div.card-body
        h5.card-title Card title
        p.card-text This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.
        +textLastUpdated
    p.mt-3.mb-1
      code img.card-img-bottom
    div.card
      div.card-body
        h5.card-title Card title
        p.card-text This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.
        +textLastUpdated
      img.card-img-bottom(src="../img/1000x100.svg" alt="Card image cap")
    h4.mt-4#image-overlays
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#image-overlays") Image overlays
    p.my-2 Use 
      code div.card-img-overlay
      |  to overlay text within an image.
    div.card.bg-dark.text-white.mt-1
      img.card-img(src="../img/1000x400.svg" alt="Card image cap")
      div.card-img-overlay
        h5.card-title Card title
        p.card-text This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.
        p.card-text Last updated 3 mins ago
    h4.mt-4#background-and-color
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#background-and-color") Background and color
    p.mb-0 Examples using 
      a(href="utilities-2.html#colors") Colors utilities
      |  classes
    div.row
      each c in contexts
        - let textCol = (c === "light") ? "" : ".text-white"
        div.col-md-6.col-xl-4
          p.mt-2.mb-0
            code= `div.card${textCol}.bg-${c}`
          div(class=`card${textCol.replace(/\./, " ")} bg-${c}` style="max-width: 18rem;")
            div.card-header Header
            div.card-body
              h5.card-title= `${c[0].toUpperCase()}${c.slice(1)} card title`
              p.card-text Some quick example text to build on the card title and make up the bulk of the card's content.
    h4.mt-4#border
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#border") Border
    p.mb-0 Examples using 
      a(href="utilities-1.html#borders") Borders utilities
      |  (see 
      a(href="utilities-1.html#borders-border-color") Border color
      |  )
    div.row
      each c in contexts
        div.col-md-6.col-xl-4
          p.mt-2.mb-0
            code= `div.card.border-${c}`
          div(class=`card border-${c}` style="max-width: 18rem;")
            div.card-header Header
            div(class=`card-body${(c === "light") ? "" : ` text-${c}`}`)
              h5.card-title= `${c[0].toUpperCase()}${c.slice(1)} card title`
              p.card-text Some quick example text to build on the card title and make up the bulk of the card's content.
    h4.mt-4#mixins-utilities
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#mixins-utilities") Mixins utilities
    p.mt-2.mb-3 You can set different colors to each subcomponents as header, footer, etc. Furthermore, using 
      code .bg-transparent
      |  removes default background color. Bellow are variations to set header/footer background colors.
    +collapseButton("#example-mixins-utilities-structure") Structure
    ul.struct.mt-2.collapse#example-mixins-utilities-structure
      li
        code div.card.border-success
        ul
          li
            code div.card-header
            |  with background variations (default, all contexts, and transparent)
          li
            code p.card-body.text-success
          li
            code div.card-footer
            |  with background variations (default, all contexts, and transparent)
    - const backgrounds = contexts.slice()
    - backgrounds.unshift("default")
    - backgrounds.push("transparent")
    div.row.mt-3
      each c in backgrounds
        div.col-md-6.col-xl-4
          p.mt-2.mb-0
            - if (c == "default")
              | (Default background)
            - else
              code= `.bg-${c}`
          - let cText = ["default","light","transparent"].indexOf(c) == -1 ? " text-white" : ""
          div.card.border-success(style="max-width: 18rem;")
            div(class=`card-header bg-${c}${cText} border-success`)
              = `${c[0].toUpperCase()}${c.slice(1)} header`
            div.card-body.text-success
              h5.card-title Success card title
              p.card-text Some quick example text to build on the card title and make up the bulk of the card's content.
            div(class=`card-footer bg-${c}${cText} border-success`)
              = `${c[0].toUpperCase()}${c.slice(1)} footer`
    h4.mt-4#card-layout
      +extLink("https://getbootstrap.com/docs/4.1/components/card/#card-layout") Card layout
    p.my-2 Use 
      code div.card-(group|deck|columns)
      |  to layout multiple cards.
    ul.struct.my-2
      li
        code div.card-(group|deck|columns)
        ul
          li Series of cards 
            code div.card
    - const layoutTexts = ["This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.", "This card has supporting text below as a natural lead-in to additional content.", "This is a wider card with supporting text below as a natural lead-in to additional content. This card has even longer content than the first to show that equal height action."]
    each layout in ["group", "deck"]
      p.my-1
        code= `div.card-${layout}`
      div(class=`card-${layout} mb-3`)
        each text in layoutTexts
          div.card
            +img286x180top
            div.card-body
              h5.card-title Card title
              p.card-text= text
            div.card-footer
              +lastUpdated
    p.mt-4.mb-1
      code div.card-columns
      |  (advanced) 
      +extLink("https://masonry.desandro.com/") Masonry
      | -like columns layout
    div.card-columns
      div.card
        img.card-img-top(src="../img/320x320.svg" alt="Card image cap")
        div.card-body
          h5.card-title (1) Card title that wraps to a new line
          p.card-text= layoutTexts[0]
      div.card.p-3
        blockquote.blockquote.mb-0.card-body
          p= `(2) ${lorem}`
          footer.blockquote-footer
            small.text-muted Someone famous in 
              cite(title="Source Title") Source Title
      div.card
        +img286x180top
        div.card-body
          h5.card-title (3) Card title
          p.card-text= layoutTexts[1]
          +textLastUpdated
      div.card.bg-primary.text-white.text-center.p-3
        blockquote.blockquote.mb-0
          p= `(4) ${lorem}`
          footer.blockquote-footer
            small.text-white Someone famous in 
              cite(title="Source Title") Source Title
      div.card.text-center
        div.card-body
          h5.card-title (5) Card title
          p.card-text= layoutTexts[1]
          +textLastUpdated
      div.card
        img.card-img-top(src="../img/400x300.svg" alt="Card image cap")
      div.card.p-3.text-right
        blockquote.blockquote.mb-0.card-body
          p= `(6) ${lorem}`
          footer.blockquote-footer
            small.text-muted Someone famous in 
              cite(title="Source Title") Source Title
      div.card
        div.card-body
          h5.card-title (7) Card title
          p= layoutTexts[2]
          +textLastUpdated

    +breadcrumbLevel1("Components(3): Cards")
